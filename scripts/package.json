{
  "name": "garagebase-scripts",
  "description": "Scripts de utilidad para GarageBase (siembra de datos, reglas de seguridad, tests)",
  "version": "1.0.0",
  "scripts": {
    "seed": "node seed.js",
    "gestor": "node set-gestor-claim.js",
    "test": "mocha test-rules.js --timeout 15000"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "mocha": "^10.8.2"
  }
}
